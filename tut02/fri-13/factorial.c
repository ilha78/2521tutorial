#include <stdio.h>
#include <stdlib.h>

int factorial(int n) {
    // base case

    // recursive case

    return 0;

}

int main(void) {
    int n = 7;
    printf("%d", factorial(n));

    return 0;
}